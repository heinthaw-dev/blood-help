---
quick_id: 260627-7gx
slug: add-notifications-screen-and-shared-head
description: Add Notifications screen + shared header bell on all 3 tab screens
status: in-progress
date: 2026-06-27
mode: quick (inline execution — gsd agents not installed)
builds_on: 260627-0lt (ScreenHeader + LanguageToggle)
---

# Quick Task 260627-7gx — Notifications screen + shared header bell

## Goal
Put the notification (bell) header on all three tab screens (Home already had it; add to
Leaderboard + Profile) and make the bell open a new Notifications screen. The screen, for now,
is a calm centered empty state ("There are no notifications for now").

## New files
- `src/components/NotificationBell.tsx` — tappable button wrapping the bell icon; used as
  ScreenHeader's `right` slot on the 3 tab screens (so the bell isn't re-inlined 3×). Prop: `onClick`.
- `src/screens/Notifications.tsx` — `phone-entry-stage/card` shell, `<ScreenHeader variant="nav" onBack title>`
  + flex-centered muted empty line. Props: `lang`, `onBack`. Bilingual (my/en). No BottomNav.

## Navigation (src/App.tsx)
- Screen union += `'notifications'`.
- `notificationsReturn` state remembers the originating tab; `handleOpenNotifications` sets it + navigates.
- Render branch returns `<Notifications onBack={() => setScreen(notificationsReturn)} />`.
- `onOpenNotifications={handleOpenNotifications}` passed to Home, Leaderboard, Profile.

## Screen wiring
- Home: replace inline bell SVG in ScreenHeader right with `<NotificationBell>`.
- Leaderboard + Profile: add `right={<NotificationBell>}` to their brand/left ScreenHeader.
- Each gains an `onOpenNotifications: () => void` prop.

## Verification
- `npm run build` (tsc -b && vite build) — no type errors.
- `npm run lint` — no NEW problems beyond the known 5-item baseline.

## Tasks
1. Create NotificationBell + Notifications.
2. Wire App.tsx navigation; thread onOpenNotifications into the 3 tab screens + the bell.
3. Build + lint.
