---
phase: quick-260627-8gf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/DonorThankYou.tsx
  - src/screens/Profile.tsx
  - src/screens/Home.tsx
autonomous: true
requirements: [QUICK-8gf]

must_haves:
  truths:
    - "In Burmese (my) mode, DonorThankYou shows only Burmese text — no English secondary lines under headline, subheadline, body, or CTA"
    - "In Burmese (my) mode, Profile shows only Burmese text — no English '...En' secondary lines and no English nudge subtitle"
    - "In Burmese (my) mode, Home shows only Burmese text — availability sub-line, setup nudge, active-request lines, feed label, and empty-state hint are Burmese-only"
    - "In English (en) mode, all three screens still display their English primary text unchanged"
    - "npm run build (tsc -b && vite build) passes with no unused-locals/params errors"
    - "npm run lint passes"
  artifacts:
    - path: "src/screens/DonorThankYou.tsx"
      provides: "Single-language donor thank-you screen (no secondary translation lines)"
      contains: "headline"
    - path: "src/screens/Profile.tsx"
      provides: "Single-language profile screen (no '...En' secondary lines)"
      contains: "donatedLine"
    - path: "src/screens/Home.tsx"
      provides: "Single-language home screen (no English-in-Burmese secondary lines)"
      contains: "availTitle"
  key_links:
    - from: "src/screens/DonorThankYou.tsx"
      to: "active-language render"
      via: "s = t[lang]; only primary fields rendered"
      pattern: "s\\.(headline|subheadline|body|cta)"
---

<objective>
Remove leaking English secondary "translation" lines from three screens (DonorThankYou, Profile, Home) so each screen displays only the active language. In Burmese (`my`) mode the secondary lines were hardcoded English (or English-mixed) and leaked onto the screen even when the user chose Burmese. After this change, each screen shows the primary (active-language) line only; English mode keeps its English primary text.

Purpose: Honor the Burmese-first design constraint — no English bleed-through when the user is in Burmese mode.

Output: Three edited screen files, secondary-line JSX and now-dead translation sub-fields removed, build + lint green.
</objective>

<execution_context>
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/skills/frontend-design/SKILL.md

# Target files (already analyzed — exact leak sites noted in tasks below)
@src/screens/DonorThankYou.tsx
@src/screens/Profile.tsx
@src/screens/Home.tsx

<notes>
- tsconfig.app.json enforces `noUnusedLocals: true` and `noUnusedParameters: true`. Any
  variable or object field that becomes unused after removing a JSX block MUST be deleted,
  or `tsc -b` (part of `npm run build`) will fail.
- This is a UI-copy / dead-code cleanup. Do NOT change behavior of the primary
  (active-language) text. Only remove the secondary line and its now-unused source fields.
- Both `my` and `en` halves of each `t` object must drop the removed fields so the object
  shape stays symmetric and no field is left dangling.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip secondary translation lines from DonorThankYou</name>
  <files>src/screens/DonorThankYou.tsx</files>
  <action>
Remove the four secondary "Sub" translation lines and their backing fields:
- Delete the four secondary-line JSX `<div>`/`<p>` blocks that render `{s.headlineSub}` (~lines 109-111), `{s.subheadlineSub}` (~lines 117-119), `{s.bodySub}` (~lines 133-135), and `{s.ctaSub}` (~lines 143-145). Keep the primary `{s.headline}`, `{s.subheadline}`, `{s.body}`, `{s.cta}` elements intact.
- Delete the now-unused sub-fields `headlineSub`, `subheadlineSub`, `bodySub`, `ctaSub` from BOTH the `my` and `en` objects in the `t` map.
- Delete the `altFont` variable declaration (line 19: `const altFont = ...`). It is only consumed by the removed secondary lines; leaving it triggers a `noUnusedLocals` build failure.
- Leave `bodyFont` and every primary field (`headline`, `subheadline`, `bloodTypeLabel`, `body`, `cta`) unchanged.
  </action>
  <verify>
    <automated>cd /Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old && grep -cE 'Sub|altFont' src/screens/DonorThankYou.tsx</automated>
  </verify>
  <done>Grep returns 0 (no `Sub` fields or `altFont` remain). Burmese mode shows only Burmese headline/subheadline/body/CTA; English mode shows only English primaries. File still exports `DonorThankYou` and renders the heart icon, blood-type badge, and CTA button.</done>
</task>

<task type="auto">
  <name>Task 2: Strip '...En' and nudge-sub secondary lines from Profile</name>
  <files>src/screens/Profile.tsx</files>
  <action>
Remove the English secondary lines that leak in `my` mode:
- Delete the guarded JSX blocks: `{t.donatedLineEn && (...)}` (~lines 185-189), `{t.lastLineEn && (...)}` (~lines 207-211), `{t.cooldownLineEn && (...)}` (~lines 228-232), `{t.qrCaptionEn && (...)}` (~lines 247-251), and `{t.emergencyHelpEn && (...)}` (~lines 328-332). Keep the primary siblings (`t.donatedLine`, `t.lastLine`, `t.cooldownLine`, `t.qrCaption`, `t.emergencyHelp`).
- Delete the unconditional nudge subtitle `<div>` that renders `{t.nudgeSub}` (~lines 289-291). Keep `{t.nudgeTitle}` above it.
- Delete the corresponding fields from BOTH `my` and `en` objects: `donatedLineEn`, `lastLineEn`, `cooldownLineEn`, `qrCaptionEn`, `emergencyHelpEn`, and `nudgeSub`. (In `en` these are empty strings already; remove them too to keep the object shape symmetric.)
- Do not touch `editSub` (a legitimate Burmese subtitle in `my`, English in `en` — single-language, not a leak). Leave all toggles, QR code, divider, and logout logic unchanged.
  </action>
  <verify>
    <automated>cd /Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old && grep -cE 'donatedLineEn|lastLineEn|cooldownLineEn|qrCaptionEn|emergencyHelpEn|nudgeSub' src/screens/Profile.tsx</automated>
  </verify>
  <done>Grep returns 0. Burmese mode shows no English under donation count, last-donation, cooldown, QR caption, emergency help, or donor nudge. English mode primaries unchanged. QR card, settings toggles, and logout still render.</done>
</task>

<task type="auto">
  <name>Task 3: Make Home secondary lines single-language (empty-state, avail, nudges, feed)</name>
  <files>src/screens/Home.tsx</files>
  <action>
Fix the English-in-Burmese secondary lines in the `my` object and the empty-state hint:
- `availOn`/`availOff` in `my`: change from `'Available to donate · အသင့်ရှိသည်'` / `'Available to donate · မရရှိနိုင်ပါ'` to Burmese-only `'အသင့်ရှိသည်'` / `'မရရှိနိုင်ပါ'`. Leave the `en` variants (`'Tap to mark unavailable'` / `'Tap to mark available'`) unchanged. This sub-line is rendered conditionally (`available ? t.availOn : t.availOff`), so it stays — just make the `my` copy Burmese.
- Remove the setup-nudge secondary line: delete the `<div>` rendering `{t.setupSub}` (~lines 446-448) and remove the `setupSub` field from both `my` and `en`. Keep `{t.setupTitle}`.
- Remove the active-request secondary line: delete the `<div>` rendering `{t.activeSub}` (~lines 483-485) and remove the `activeSub` field from both `my` and `en`. Keep `{t.activeTitle}`.
- Remove the activity secondary line: delete the `<div>` rendering `{t.activitySub}` (~lines 504-506) and remove the `activitySub` field from both `my` and `en`. Keep `{t.activityLine}`.
- Remove the feed secondary label: delete the `{lang === 'my' && (...)}` block that renders `{t.feedSub}` next to the section title (~lines 603-607) and remove the `feedSub` field from both `my` and `en`. Keep `{t.feedTitle}`.
- Empty-state hint: the render uses `{t.emptyHint}` (~line 622). In `my`, `emptyHint` is English (`"We'll alert you the moment someone does."`). Change the render to use `{t.emptySub}` instead (Burmese: `'လိုအပ်သည့်အခါ ချက်ချင်း အကြောင်းကြားပါမည်။'` in `my`, and for `en` set `emptySub` to its English copy `"We'll alert you the moment someone needs help."`). Then delete the now-unused `emptyHint` field from both `my` and `en`. Net result: empty state shows Burmese-only in `my`, English-only in `en`.
- Leave every primary Burmese label (`availTitle`, `setupTitle`, `activeTitle`, `activityLine`, `viewBtn`, `requestBtn`, `feedTitle`, `emptyTitle`, `urgentLabel`, `callLabel`, `helpLabel`) and all RequestCard / feed logic untouched.
  </action>
  <verify>
    <automated>cd /Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old && grep -cE 'setupSub|activeSub|activitySub|feedSub|emptyHint|Available to donate ·' src/screens/Home.tsx</automated>
  </verify>
  <done>Grep returns 0. Burmese mode: availability sub-line, setup nudge, active-request lines, feed section label, and empty-state hint are all Burmese-only. English mode unchanged. Feed RPC, RequestCard, and FCM alert logic intact.</done>
</task>

</tasks>

<verification>
After all three tasks, run the full build and lint gates from the repo root:

```
cd /Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old && npm run build && npm run lint
```

`npm run build` runs `tsc -b && vite build` — this catches any `noUnusedLocals`/`noUnusedParameters`
violations from a removed field whose backing variable/JSX was missed. Both commands must exit 0.

Manual spot-check (no automated browser test per project memory — user tests manually):
- Toggle language to Burmese on Home, Profile, and the Donor Thank You screen → confirm no English
  appears under any Burmese label.
- Toggle to English → confirm English primary copy still renders on all three screens.
</verification>

<success_criteria>
- DonorThankYou, Profile, and Home each render only the active language's text (no secondary translation lines).
- `headlineSub`/`subheadlineSub`/`bodySub`/`ctaSub`/`altFont` gone from DonorThankYou.
- `donatedLineEn`/`lastLineEn`/`cooldownLineEn`/`qrCaptionEn`/`emergencyHelpEn`/`nudgeSub` gone from Profile.
- `setupSub`/`activeSub`/`activitySub`/`feedSub`/`emptyHint` gone from Home; `availOn`/`availOff` Burmese-only in `my`; empty state uses `emptySub`.
- English mode primary copy unchanged on all three screens.
- `npm run build` and `npm run lint` both pass.
</success_criteria>

<output>
Create `.planning/quick/260627-8gf-remove-leaking-english-secondary-transla/260627-8gf-SUMMARY.md` when done.
</output>
