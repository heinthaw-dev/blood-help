---
quick_id: 260630-43p
slug: design-top-tier-readme
description: Design and write a polished, top-tier README.md for the Blood Help PWA
date: 2026-06-30
mode: quick (manual — gsd-sdk CLI unavailable, bookkeeping replicated by hand)
status: complete
---

# Quick Task 260630-43p: Design top-tier README.md

## Goal

Replace the default Vite-template `README.md` with a polished, recruiter-grade
README that serves as the public "gate" of the repo. Must be accurate to the
**actual shipped state** (v2.0 Backend Core complete — Supabase + realtime + FCM
push + PWA on Vercel), not the stale "UI-only, no backend" framing in CLAUDE.md.

## Context Notes

- Current `README.md` is the unedited Vite starter template (73 lines) — full replace.
- `package.json` shows the real stack is installed: `@supabase/supabase-js`,
  `firebase`, `react-qr-code`, `react-zxing`, `vite-plugin-pwa`,
  `@vitejs/plugin-basic-ssl`. CLAUDE.md is out of date on this point.
- STATE.md: v2.0 milestone complete, E2E loop verified
  (request → nearby compatible donors alerted → donor responds → requester
  confirms via QR scan → congrats + leaderboard credit).
- 14 flow screenshots already committed under `screenshots/` — used for the
  two user-flow walkthroughs (donor + requester).
- Env surface: `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY`, 7× `VITE_FIREBASE_*`.
- Genuinely deferred (keep Roadmap honest): Twilio SMS OTP (v4), data purge +
  gated phone reveal (v3), `react-i18next`.

## Tasks

### Task 1 — Write README.md
- **files:** `README.md`
- **action:** Fully replace the Vite-template README with the following sections,
  in order: hero (title + tagline + tech badges), Table of Contents, What it is,
  The problem it solves + advantages, User flows with screenshots (shared
  auth prefix shown once, then donor + requester branches), How it's built
  (tech stack + features + architecture), Getting Started (prereqs, install,
  `.env` keys, npm scripts), Project Structure, Roadmap / status, Contributing,
  License, closing "built for Myanmar" note.
- **verify:** Markdown renders; all `screenshots/*.png` paths resolve; tech-stack
  claims match `package.json`; tone is emergency-calm and mission-driven.
- **done:** `README.md` no longer contains Vite-template boilerplate and covers
  all four user-requested sections plus the senior-engineer additions.

## Out of Scope
- No source-code changes; docs only.
- No new screenshots captured (existing 14 reused).
