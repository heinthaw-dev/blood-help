---
phase: 08-donor-response-realtime
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/screens/Home.tsx
  - src/App.tsx
autonomous: true
requirements: [DNOR-01]
user_setup: []

must_haves:
  truths:
    - "Each Home feed card's right-side action slot is a state machine: before responding it is an 'I'll help' (ကူညီမည်) labeled pill; after responding it becomes the round red call button AND a green '✓ ကူညီမည်' tag appears by the address (D-01 single state-driven slot — no second button added)"
    - "The requester's phone number is hidden on the card until the donor has responded; it is revealed alongside the call button only after 'I'll help' is tapped (D-02)"
    - "Tapping 'I'll help' flips the slot to the responded state immediately (optimistic); a real DB failure rolls the slot back and surfaces the existing AlertDialog; error code 23505 is a SILENT no-op that keeps the responded state (D-03 optimistic; D-04 backstop; NOT the Phase-7 duplicate dialog)"
    - "On Home feed load, the current donor's own request_responses rows (status='responding') are fetched and matching cards start in the responded state across reload/app-reopen (D-04)"
    - "An 'I'll help' tap inserts exactly one request_responses row with donor_id = auth.uid() and status='responding'"
  artifacts:
    - path: "src/App.tsx"
      provides: "handleRespond optimistic insert handler with 23505 no-op + rollback, and respondedIds state passed to Home"
      contains: "request_responses"
    - path: "src/screens/Home.tsx"
      provides: "State-driven RequestCard action slot (I'll help pill ↔ call button + responded tag), number-hide, responded-state restore query"
      contains: "ကူညီမည်"
  key_links:
    - from: "src/App.tsx handleRespond"
      to: "request_responses table"
      via: "supabase.from('request_responses').insert({ request_id, donor_id })"
      pattern: "request_responses"
    - from: "src/screens/Home.tsx feed load effect"
      to: "request_responses (own rows)"
      via: "supabase.from('request_responses').select('request_id').eq('donor_id', uid).eq('status','responding')"
      pattern: "request_responses"
    - from: "src/screens/Home.tsx RequestCard"
      to: "respondedIds + onRespond props"
      via: "props from App"
      pattern: "respondedIds|onRespond"
---

<objective>
Implement the donor-side "I'll help" action on the Home feed (DNOR-01). The feed card's existing right-side action slot becomes a state machine (D-01): an "I'll help" (ကူညီမည်) pill before responding, and the round red call button + a green "✓ ကူညီမည်" tag after responding. The requester's phone is hidden until the donor responds (D-02). The tap is optimistic with rollback on real failure and a silent no-op on the duplicate (23505) path (D-03/D-04). On feed load, the donor's own existing responses pre-mark cards as responded so state survives reload (D-04).

Purpose: This is the donor half of the live loop — the action that creates the `request_responses` row Plan 03's requester screen will render live.
Output: Updated `src/screens/Home.tsx` (state-driven card slot + restore query) and `src/App.tsx` (handleRespond + respondedIds state).
</objective>

<execution_context>
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-donor-response-realtime/08-CONTEXT.md
@.planning/phases/08-donor-response-realtime/08-RESEARCH.md
@src/screens/Home.tsx
@src/App.tsx
@CLAUDE.md
@.claude/skills/frontend-design/SKILL.md

<interfaces>
<!-- Home.tsx existing (reuse, do not duplicate): -->
<!--   NearbyRequest = { id, bloodType, currentAddress, distMeters, createdAt, urgent, phone } -->
<!--   formatPhone(e164) → "09-XXX-XXX-XXX"   (already in Home.tsx) -->
<!--   RequestCard({ req, lang, urgentLabel, callLabel }) — the slot to convert into a state machine -->
<!--   the existing tel: call <a> at the end of RequestCard is the "after responded" affordance -->
<!-- RequestLive.tsx green responder pill to REUSE VERBATIM for the "✓ responded" tag (line ~318-324): -->
<!--   <span style={{ fontFamily:'var(--font-burmese)', fontSize:11, fontWeight:600, lineHeight:1, whiteSpace:'nowrap', -->
<!--     color:'var(--color-success)', background:'var(--color-success-tint)', borderRadius:'var(--radius-pill)', padding:'4px 8px' }}>ကူညီမည်</span> -->
<!-- App.tsx existing patterns to MIRROR: -->
<!--   user.supabaseId (auth uid), writeError state + writeErrorStrings (D-18 AlertDialog), -->
<!--   bare supabase.from(...).insert({...}) with `if (error) { if (error.code === '23505') ... }` (handlePosted, line ~309-337) -->
<!--   Home is rendered in the screen==='home' branch (App.tsx ~531) — add new props there -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add handleRespond + respondedIds state in App.tsx (optimistic insert, 23505 no-op, rollback)</name>
  <read_first>
    - src/App.tsx (handlePosted lines ~299-341 — the bare-insert + 23505 branch convention to MIRROR but invert: here 23505 is a no-op, NOT a duplicate dialog; writeError/writeErrorStrings for the generic-failure dialog; the screen==='home' Home render block ~531-548 where new props are wired; hydrateUserFromDb for where respondedIds is hydrated if needed)
    - .planning/phases/08-donor-response-realtime/08-RESEARCH.md (Pattern 4 bare insert; Pattern 5 — 23505 = already-responded SILENT no-op, keep responded state, NO AlertDialog; Pitfall 3 — do not copy the Phase 7 duplicate-dialog branch; Code Examples "I'll help handler")
    - .planning/phases/08-donor-response-realtime/08-CONTEXT.md (D-03 optimistic + rollback, D-04 restore)
  </read_first>
  <action>
    In `src/App.tsx`, add a `respondedIds` state as a `Set<string>` of request ids the current donor has responded to. Add an async `handleRespond(reqId: string)` handler that: (1) optimistically adds reqId to respondedIds; (2) returns early if `user.supabaseId` is falsy; (3) performs a bare `supabase.from('request_responses').insert({ request_id: reqId, donor_id: user.supabaseId })` (omit `status` — it defaults to `'responding'`; no `.select()`/`.single()` chaining per the codebase convention); (4) on error, if `error.code === '23505'` do NOTHING (the donor already responded — keep the optimistic responded state, do NOT show any dialog — this is the OPPOSITE of handlePosted's 23505 branch, which shows the duplicate dialog; see Pitfall 3); (5) on any other error, remove reqId from respondedIds (rollback) and set `writeError` to the generic `genericTitle`/`genericMsg` from `writeErrorStrings[lang]` (reuse the existing AlertDialog, D-03/D-18).

    Hydrate respondedIds so it survives reload (D-04): in `hydrateUserFromDb`, after the existing reads, fetch `supabase.from('request_responses').select('request_id').eq('donor_id', uid).eq('status', 'responding')` (RLS allows a donor to read their own rows) and `setRespondedIds(new Set(rows.map(r => r.request_id)))`. (Placing the restore query here keeps it co-located with the other mount hydration; alternatively pass a callback to Home to load it on feed mount — either is acceptable, but the data MUST be present before the cards first render so the slot starts in the correct state.)

    Pass two new props into the existing `<Home … />` render in the `screen === 'home'` branch: `respondedIds={respondedIds}` and `onRespond={handleRespond}`.
  </action>
  <verify>
    <automated>cd /Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old && grep -c "handleRespond" src/App.tsx && grep -c "request_responses" src/App.tsx && grep -c "23505" src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleRespond" src/App.tsx` ≥ 2 (definition + wiring into Home).
    - `grep -c "request_responses" src/App.tsx` ≥ 2 (the insert in handleRespond + the restore select in hydrateUserFromDb).
    - The handleRespond 23505 branch contains NO call to `setWriteError` (verifiable by reading: the 23505 path is empty/no-op, only the else branch rolls back + sets writeError).
    - The `<Home` render block in the `screen === 'home'` branch passes `respondedIds` and `onRespond` props.
    - `npm run build` exits 0 (no unused-locals/params violations).
  </acceptance_criteria>
  <done>App.tsx owns respondedIds, inserts a response optimistically with a correct 23505 no-op and rollback, restores responded state on hydration, and passes both new props to Home.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert RequestCard's action slot into a state machine in Home.tsx (I'll help pill ↔ call + responded tag, number-hide)</name>
  <read_first>
    - src/screens/Home.tsx (RequestCard lines ~76-173 — the existing tel: call <a> slot to make conditional; the phone row lines ~139-146 to gate behind responded; the HomeProps interface ~177-198 and the t.{my,en} string block ~259-300 to add the "I'll help" label; the requests.map render ~558-560 that must pass through responded state + onRespond)
    - src/screens/RequestLive.tsx (lines ~301-335 — the green responder pill markup to REUSE VERBATIM for the "✓ ကူညီမည်" responded tag; reuse its exact color/background/radius/padding tokens for one consistent visual language, D-01)
    - .planning/phases/08-donor-response-realtime/08-CONTEXT.md (D-01 state-driven single slot, label ကူညီမည်; D-02 hide number until responded)
    - .claude/skills/frontend-design/SKILL.md (emergency-calm tone, never hardcode hex except #fff/scrim, Burmese-first, var(--color-*) tokens)
  </read_first>
  <action>
    In `src/screens/Home.tsx`, extend `RequestCardProps` and `HomeProps` so the card knows whether this request is responded and how to respond: add `responded: boolean` and `onRespond: () => void` to RequestCardProps; add `respondedIds: Set<string>` and `onRespond: (reqId: string) => void` to HomeProps (and destructure them in the Home function signature). In the `requests.map(...)` render, pass `responded={respondedIds.has(req.id)}` and `onRespond={() => onRespond(req.id)}` to each RequestCard.

    Inside RequestCard, make the right-side action slot a state machine driven by `responded` (D-01):
    - When `responded === false`: render an "I'll help" labeled PILL button (not just an icon) using the Burmese label **ကူညီမည်** with an English secondary per the t-strings (add `helpLabel` to both `t.my` and `t.en` — `my: 'ကူညီမည်'`, `en: "I'll help"`). Style it as a primary-tint or primary pill consistent with the design system (use `var(--color-primary)` / `var(--color-primary-tint)` tokens, `var(--radius-pill)` or `var(--radius-button)`, `var(--font-burmese)`). Its onClick calls `onRespond()`. Do NOT add a second button — this replaces the call <a> in the slot for the not-yet-responded state.
    - When `responded === true`: render the existing round red call <a> (the current `tel:${req.phone}` affordance) in the slot, AND render a small green **✓ ကူညီမည်** tag next to the address — reuse the RequestLive green responder pill markup verbatim (success color + success-tint background + radius-pill + the 4px/8px padding). 

    Number-hide (D-02): the phone row (the small `formatPhone(req.phone)` line, ~139-146) MUST only render when `responded === true`. Before responding, the requester's number is not shown on the card at all.

    Keep all copy bilingual (EN + Burmese) and use only CSS token variables for color (no hardcoded hex except `#fff`).
  </action>
  <verify>
    <automated>cd /Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old && grep -c "responded" src/screens/Home.tsx && grep -c "ကူညီမည်" src/screens/Home.tsx && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "responded" src/screens/Home.tsx` ≥ 3 (prop on RequestCardProps, prop on HomeProps usage, and the conditional render).
    - `grep -c "ကူညီမည်" src/screens/Home.tsx` ≥ 1 (the I'll help label and/or the responded tag).
    - The `formatPhone(req.phone)` phone row is wrapped in a `responded &&` (or equivalent) conditional — it does not render in the not-responded state (D-02).
    - No second standalone action button is added: in the not-responded state the slot is the I'll help pill; in the responded state it is the call <a> (the slot holds exactly one action at a time).
    - The responded green tag reuses `var(--color-success)` + `var(--color-success-tint)` (no hardcoded hex).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>The Home feed card slot toggles between the "I'll help" pill and the call button + green responded tag, hides the number until responded, and the requests.map passes responded state and onRespond through.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → request_responses INSERT | The browser submits a response row; donor_id is attacker-suppliable in the request body |
| client → request_responses SELECT (own rows) | The browser reads its own responses to restore card state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Spoofing / Elevation of Privilege | Forged `donor_id` on insert (respond as someone else) | mitigate | The existing Phase 6 INSERT policy `WITH CHECK (auth.uid() = donor_id)` rejects any row whose donor_id ≠ the caller. handleRespond sends `donor_id: user.supabaseId` (the session uid), but the DB is the authoritative enforcer. |
| T-08-06 | Tampering | Duplicate / spam responses via double-tap or repeated taps | mitigate | The deployed `unique (request_id, donor_id)` constraint makes the second insert error 23505; the client treats 23505 as a silent no-op (D-04 backstop). The optimistic flip + responded-state restore also hide/disable the action once responded. |
| T-08-07 | Information Disclosure | Requester phone shown on the card before the donor commits | mitigate | D-02: the phone row is gated behind `responded === true`; pre-response cards never render the number. (The phone is non-sensitive requester contact, but commit-then-contact is the intended posture.) |
| T-08-08 | Information Disclosure (UX-correctness) | 23505 surfacing the wrong dialog and leaking confusing state | mitigate | Pitfall 3: 23505 is a no-op here, not the Phase-7 duplicate dialog; the handler keeps responded state silently. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `grep -c "handleRespond" src/App.tsx` ≥ 2; `grep -c "request_responses" src/App.tsx` ≥ 2; `grep -c "23505" src/App.tsx` ≥ 1.
- `grep -c "responded" src/screens/Home.tsx` ≥ 3; `grep -c "ကူညီမည်" src/screens/Home.tsx` ≥ 1.
- `npm run build` green.
- Manual (Supabase dashboard): tap "I'll help" → one request_responses row appears with status='responding' and donor_id = the user's uid; the slot shows the call button + green tag and the number; tap again (or reload) → no new row, no dialog, card stays responded.
</verification>

<success_criteria>
DNOR-01 satisfied: the Home card's single action slot is state-driven, "I'll help" creates a `request_responses` row (status='responding') optimistically with correct 23505 no-op and rollback, the number is hidden until responded, and responded state survives reload via the restore query.
</success_criteria>

<output>
Create `.planning/phases/08-donor-response-realtime/08-02-SUMMARY.md` when done
</output>
