---
phase: 07-data-persistence-geo-matching
plan: 04
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - src/screens/CreateRequest.tsx
  - src/screens/Home.tsx
autonomous: true
requirements: [BACK-06, GEO-01, GEO-02]

must_haves:
  truths:
    - "The request form's address field is required — the post button stays disabled until current_address is non-empty, and the 'Optional' marker no longer applies to it (D-09)"
    - "The home feed shows real active blood_requests within 10 km of the donor's coarsened location via the requests_within_radius RPC — not DUMMY_REQUESTS (GEO-02)"
    - "The feed is filtered to request types the viewing donor can donate into via COMPATIBLE_REQUEST_TYPES (directional, GEO-01) — an O+ donor sees O+/A+/B+/AB+ requests, not only O+"
    - "The user's own active request is excluded from the feed (requester_id != currentUserId)"
    - "Feed cards show distance (from dist_meters) and time-ago (from created_at) with Burmese numerals via formatNumber"
    - "When donor lat/lng are null the RPC is not called and the empty state renders (Pitfall 6)"
  artifacts:
    - path: "src/screens/Home.tsx"
      provides: "Live feed via requests_within_radius RPC + compatibility + own-request filter; NearbyRequest refactored to currentAddress/distMeters/createdAt"
      contains: "requests_within_radius"
    - path: "src/screens/CreateRequest.tsx"
      provides: "current_address required (postDisabled includes empty-address guard); Optional marker removed"
      contains: "address.trim()"
  key_links:
    - from: "src/screens/Home.tsx loadFeed"
      to: "requests_within_radius RPC"
      via: "supabase.rpc('requests_within_radius', { lat, lng, radius_km })"
      pattern: "requests_within_radius"
    - from: "src/screens/Home.tsx feed filter"
      to: "src/blood.ts COMPATIBLE_REQUEST_TYPES"
      via: ".filter(r => COMPATIBLE_REQUEST_TYPES[donorBloodType].includes(r.blood_type))"
      pattern: "COMPATIBLE_REQUEST_TYPES"
    - from: "src/screens/CreateRequest.tsx postDisabled"
      to: "current_address required"
      via: "address.trim().length === 0 guard"
      pattern: "address\\.trim"
---

<objective>
Make the request form's address required and replace the home feed's static DUMMY_REQUESTS with a live, proximity- and compatibility-filtered query against the migrated DB.

Purpose: D-09 (required current_address), GEO-02 (real feed within 10 km via requests_within_radius), GEO-01 (directional compatibility filter), and own-request exclusion. Depends on plan 01 (RPC + types + current_address), plan 02 (COMPATIBLE_REQUEST_TYPES), and plan 03 (App passes donorLat/donorLng/currentUserId/donorBloodType props to Home).

Output: CreateRequest with required address; Home with live feed.
</objective>

<execution_context>
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-data-persistence-geo-matching/07-CONTEXT.md
@.planning/phases/07-data-persistence-geo-matching/07-RESEARCH.md
@.planning/phases/07-data-persistence-geo-matching/07-PATTERNS.md
@.planning/phases/07-data-persistence-geo-matching/07-01-SUMMARY.md
@.planning/phases/07-data-persistence-geo-matching/07-03-SUMMARY.md
@blood-help-spec.md
@CLAUDE.md
@src/screens/CreateRequest.tsx
@src/screens/Home.tsx
@src/blood.ts
@src/i18n.ts
@src/lib/supabase.ts
@src/types/database.ts

<interfaces>
From src/blood.ts (added in plan 02):
```typescript
export const COMPATIBLE_REQUEST_TYPES: Record<BloodType, BloodType[]>
```
From src/i18n.ts (existing — reuse for Burmese numerals):
```typescript
export function formatNumber(n: number, lang: Lang): string
```
From src/types/database.ts (regenerated in plan 01):
```typescript
// Functions.requests_within_radius.Returns rows:
// { id, requester_id, blood_type, current_address, contact_phone, units_needed, units_collected, urgency, status, created_at, expires_at, dist_meters }
```
From src/App.tsx (props passed in plan 03 — confirm exact names in 07-03-SUMMARY):
```typescript
// Home receives: donorLat, donorLng, currentUserId, donorBloodType
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make current_address required in CreateRequest</name>
  <read_first>
    - src/screens/CreateRequest.tsx (full file — the address state, postDisabled guard at ~line 52, the "Optional" divider at ~lines 326–342, the strings objects with optional copy at ~lines 55–99)
    - 07-PATTERNS.md §src/screens/CreateRequest.tsx (exact postDisabled change, the optional divider to remove, the optional strings to remove)
    - 07-CONTEXT.md D-09 (address now required; flip the Optional marking)
    - CLAUDE.md (inline styles via tokens, named export + default alias, Burmese-first)
  </read_first>
  <action>
    Update the postDisabled guard to also require a non-empty address: add `|| address.trim().length === 0` to the existing `!bloodType || phone.replace(/\D/g,'').length === 0` expression (D-09). Remove the "Optional" divider block (the marginTop:28 flex row rendering copy.optional between address and units) since the address above it is now required — either delete it entirely or reposition it to mark only the units/urgency section as optional; prefer deletion for clarity unless units/urgency genuinely need an optional marker. Remove or repurpose the `optional` string from both the my and en strings objects accordingly. Do NOT change the GeoPhase machine or onPosted callback — those are already wired and App.handlePosted (plan 03) performs the write. Keep current_address terminology (the column was renamed in plan 01); ensure no `township` string remains in this file.
  </action>
  <acceptance_criteria>
    - `grep -c "address.trim" src/screens/CreateRequest.tsx` returns >= 1 and the postDisabled expression includes the empty-address guard (source assertion: post button is disabled when address is blank)
    - The "Optional" divider between address and units is removed (grep for the copy.optional render shows it no longer marks the required address field)
    - `grep -c "township" src/screens/CreateRequest.tsx` returns 0 (Pitfall 7)
    - `npm run build` passes for CreateRequest.tsx
    - Observable: with a valid blood type + phone but a blank address, the Post button is disabled; filling the address enables it
  </acceptance_criteria>
  <done>Address is required to post a request; the Optional marker no longer applies to it; no stale township reference.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace DUMMY_REQUESTS with live requests_within_radius feed in Home</name>
  <read_first>
    - src/screens/Home.tsx (full file — the NearbyRequest interface + DUMMY_REQUESTS const at lines 10–51, formatPhone at ~54–61, the RequestCard render reading req.township[lang]/req.distance[lang]/req.timeAgo[lang], hasOpenRequest usage, HomeProps)
    - 07-PATTERNS.md §src/screens/Home.tsx (imports to add, NearbyRequest refactor, DUMMY_REQUESTS removal, loadFeed useEffect with the two .filter() passes + .map(), RequestCard derived-label changes, formatDistanceLabel/formatTimeAgo helpers, hasOpenRequest unchanged)
    - 07-RESEARCH.md Pattern 5 (loadFeed), Pattern 8 (distance/time-ago formatting), Pattern 9 (NearbyRequest shape), Pitfall 6 (null-coord guard), Pitfall 2 (radius_km not *1000 on the client — the RPC handles meters; the client passes km)
    - 07-02-SUMMARY.md (COMPATIBLE_REQUEST_TYPES export confirmation), 07-03-SUMMARY.md (exact Home prop names from App)
    - 07-CONTEXT.md GEO-01/GEO-02 discretion items (client-side compat filter, exclude own request, availability does NOT gate the feed)
    - src/i18n.ts (formatNumber for Burmese numerals)
    - CLAUDE.md + .claude/skills/frontend-design/SKILL.md (design tokens, Burmese-first labels, empty-state copy kept)
  </read_first>
  <action>
    Add imports: useState/useEffect (if not present), supabase from '../lib/supabase', COMPATIBLE_REQUEST_TYPES from '../blood', formatNumber from '../i18n'. Refactor the NearbyRequest interface to the DB-derived shape: { id: string; bloodType: BloodType; currentAddress: string; distMeters: number; createdAt: string; urgent: boolean; phone: string } — drop the bilingual Record fields (township/distance/timeAgo). Remove the DUMMY_REQUESTS const and replace feed source with `const [requests, setRequests] = useState<NearbyRequest[]>([])`. Add the new props to HomeProps and the component signature: donorLat: number | null, donorLng: number | null, currentUserId: string | null, donorBloodType: BloodType (use the exact names confirmed in 07-03-SUMMARY). Add a useEffect keyed on [donorLat, donorLng, donorBloodType, currentUserId] that: guards `if (!donorLat || !donorLng) return` (Pitfall 6 — render empty state when coords are null); uses a cancelled flag; calls `supabase.rpc('requests_within_radius', { lat: donorLat, lng: donorLng, radius_km: DISPLAY_RADIUS_KM })` with `const DISPLAY_RADIUS_KM = 10`; on error/!data/cancelled returns; then maps the rows applying two filters — `.filter(r => r.requester_id !== currentUserId)` (exclude own request) and `.filter(r => COMPATIBLE_REQUEST_TYPES[donorBloodType].includes(r.blood_type))` (directional compatibility, GEO-01) — and `.map()` into NearbyRequest (currentAddress=r.current_address, distMeters=r.dist_meters, createdAt=r.created_at, urgent=r.urgency==='urgent', phone=r.contact_phone, bloodType=r.blood_type as BloodType); setRequests(filtered). Do NOT filter by donor availability (discretion: availability gates push in Phase 8, not the feed). Add module-scope helpers formatDistanceLabel(distMeters, lang) (`~X km` for >=1km, `~X m` for <1km, Burmese numerals via formatNumber) and formatTimeAgo(createdAt, lang) (min/hour ago with Burmese numerals). Update RequestCard to read req.currentAddress (plain string), formatDistanceLabel(req.distMeters, lang), formatTimeAgo(req.createdAt, lang) instead of the old [lang]-indexed accessors. Keep formatPhone unchanged (already E.164). Keep the existing empty-state copy and the hasOpenRequest CTA-swap behavior (unchanged — driven from App). Ensure no `township` reference remains. Keep named export + default alias.
  </action>
  <acceptance_criteria>
    - `grep -c "requests_within_radius" src/screens/Home.tsx` returns >= 1 and it is called via `supabase.rpc(`
    - `grep -c "DUMMY_REQUESTS" src/screens/Home.tsx` returns 0 (static data removed); feed state is `useState<NearbyRequest[]>`
    - The feed useEffect contains both filters: `requester_id !== currentUserId` (own-request exclusion) and `COMPATIBLE_REQUEST_TYPES[donorBloodType].includes` (GEO-01) — source assertion
    - NearbyRequest interface contains `currentAddress`, `distMeters`, `createdAt` and does NOT contain `township`, `Record<Lang` for those fields
    - The RPC call passes `radius_km: DISPLAY_RADIUS_KM` where DISPLAY_RADIUS_KM === 10, and is NOT multiplied by 1000 on the client (the RPC converts to meters — Pitfall 2)
    - A null-coordinate guard exists before the RPC call (`grep` shows `if (!donorLat` or equivalent) — Pitfall 6
    - formatDistanceLabel and formatTimeAgo use formatNumber for Burmese numerals (source assertion)
    - `grep -c "township" src/screens/Home.tsx` returns 0
    - `npm run build` passes (Home compiles against regenerated database.ts RPC return types and the new App props)
    - Observable (manual, Supabase has seeded rows near 16.82/96.15): an O+ donor at the seed coordinates sees compatible requests within 10 km and not their own; an AB+ donor sees only AB+ requests (directional)
  </acceptance_criteria>
  <done>Home feed is live: requests_within_radius within 10 km, directional-compatibility filtered, own request excluded, distance/time-ago in Burmese numerals; empty state when coords unavailable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → requests_within_radius RPC | The feed query crosses into Postgres; the RPC (SECURITY DEFINER) decides what columns return |
| RPC result → rendered card | Returned requester contact_phone is shown to the donor (intended per spec §3.4); donor PII is never in the result set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Information Disclosure | Feed exposing a donor's phone or another user's private data | mitigate | requests_within_radius (plan 01) returns only requester-side feed columns; requester contact_phone is intentionally shown to donors (spec §3.4); no donor row is queried by the feed |
| T-07-12 | Tampering | Incompatible donor shown a request (wrong matrix) or own request shown | mitigate | Client filter via COMPATIBLE_REQUEST_TYPES (spec §3.1, plan 02) + requester_id !== currentUserId exclusion |
| T-07-13 | Denial of Service | RPC called with null coords causing a Postgres error / broken feed | mitigate | Null-coordinate guard before rpc() renders the empty state instead (Pitfall 6) |
| T-07-14 | Tampering | Posting a request with no locatable address (donors can't find requester) | mitigate | current_address required via postDisabled guard (D-09) — request cannot be posted blank |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (07-RESEARCH §Package Legitimacy Audit — none added); no install task present |
</threat_model>

<verification>
- CreateRequest: blank address keeps Post disabled; filled address enables it; no township string
- Home: feed renders real seeded requests within 10 km, directional-filtered, own request excluded; cards show distance + time-ago in Burmese numerals; empty state when coords null
- npm run build green
</verification>

<success_criteria>
- [ ] current_address required in CreateRequest; Optional marker no longer on the address (D-09)
- [ ] Home feed live via requests_within_radius within 10 km (GEO-02), DUMMY_REQUESTS removed
- [ ] Directional compatibility filter via COMPATIBLE_REQUEST_TYPES (GEO-01); own request excluded
- [ ] Distance + time-ago derived from dist_meters/created_at with Burmese numerals
- [ ] Null-coord guard; no stale township references; build passes
</success_criteria>

<output>
Create `.planning/phases/07-data-persistence-geo-matching/07-04-SUMMARY.md` when done.
</output>
