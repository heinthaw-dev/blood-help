---
phase: 07-data-persistence-geo-matching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/blood.ts
autonomous: true
requirements: [GEO-01]

must_haves:
  truths:
    - "blood.ts exports COMPATIBLE_REQUEST_TYPES mapping each donor blood type to the request types they can donate INTO (directional, spec §3.1)"
    - "An O- donor maps to all 8 request types (universal donor)"
    - "An AB+ donor maps only to ['AB+'] (can only donate to AB+)"
    - "The map is typed Record<BloodType, BloodType[]> using the existing BloodType union"
  artifacts:
    - path: "src/blood.ts"
      provides: "Directional blood-type compatibility lookup for the feed filter (GEO-01)"
      contains: "COMPATIBLE_REQUEST_TYPES"
  key_links:
    - from: "src/blood.ts COMPATIBLE_REQUEST_TYPES"
      to: "spec §3.1 directional matrix"
      via: "donor-keyed Record<BloodType, BloodType[]>"
      pattern: "COMPATIBLE_REQUEST_TYPES"
---

<objective>
Add the directional blood-type compatibility map to src/blood.ts so the home feed can filter requests to only those the viewing donor can actually donate into (GEO-01).

Purpose: Spec §3.1 compatibility is directional — a donor donates INTO a recipient type, never exact-match. This map is the single source of truth for that logic, consumed by Home.tsx's feed filter in plan 04. It has no schema dependency, so it runs in Wave 1 in parallel with the migration.

Output: COMPATIBLE_REQUEST_TYPES exported from src/blood.ts.
</objective>

<execution_context>
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-data-persistence-geo-matching/07-CONTEXT.md
@.planning/phases/07-data-persistence-geo-matching/07-RESEARCH.md
@.planning/phases/07-data-persistence-geo-matching/07-PATTERNS.md
@blood-help-spec.md
@src/blood.ts

<interfaces>
From src/blood.ts (existing — the pattern to extend):
```typescript
export const BLOOD_TYPES = ['A+', 'A-', 'B+', 'B-', 'O+', 'O-', 'AB+', 'AB-'] as const
export type BloodType = (typeof BLOOD_TYPES)[number]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add COMPATIBLE_REQUEST_TYPES directional map to blood.ts</name>
  <read_first>
    - src/blood.ts (the existing flat-module pattern — named export const + JSDoc, no class, no default export)
    - 07-PATTERNS.md §src/blood.ts — add COMPATIBLE_REQUEST_TYPES (the exact map values, donor-keyed)
    - 07-RESEARCH.md §Blood-Type Compatibility Matrix (recipient-first table AND the donor-first code map — note the code map is donor-keyed, which is the consuming direction in Home.tsx)
    - blood-help-spec.md §3.1 (authoritative directional compatibility table)
  </read_first>
  <action>
    Append to src/blood.ts (after the existing BLOOD_TYPES / BloodType exports, same module — do NOT create a new file) a named export `COMPATIBLE_REQUEST_TYPES: Record<BloodType, BloodType[]>` with a JSDoc comment stating: key = donor's blood type, value = request blood types the donor can donate into, source blood-help-spec.md §3.1, usage `COMPATIBLE_REQUEST_TYPES[donorBloodType].includes(requestBloodType)`. The exact directional values (donor → list of recipient request types): 'O-' → all 8 (['O-','O+','A-','A+','B-','B+','AB-','AB+']) universal donor; 'O+' → ['O+','A+','B+','AB+']; 'A-' → ['A-','A+','AB-','AB+']; 'A+' → ['A+','AB+']; 'B-' → ['B-','B+','AB-','AB+']; 'B+' → ['B+','AB+']; 'AB-' → ['AB-','AB+']; 'AB+' → ['AB+']. Use single quotes and the trailing-comma style already in the file. Follow CLAUDE.md conventions: named export, no throw, no default export needed for a const.
  </action>
  <acceptance_criteria>
    - `grep -c "COMPATIBLE_REQUEST_TYPES" src/blood.ts` returns at least 1 (the export exists)
    - The export is typed `Record<BloodType, BloodType[]>` (source assertion: the file contains `COMPATIBLE_REQUEST_TYPES: Record<BloodType, BloodType[]>`)
    - The 'O-' entry contains all 8 blood types and the 'AB+' entry contains only 'AB+' (source assertion against spec §3.1 directional values)
    - All 8 BloodType keys are present (O-, O+, A-, A+, B-, B+, AB-, AB+) — TypeScript Record<BloodType,...> enforces exhaustiveness; `npm run build` passes with no error on blood.ts
  </acceptance_criteria>
  <done>COMPATIBLE_REQUEST_TYPES exported from src/blood.ts with correct directional values; build passes for this file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure data-mapping utility; no input crosses a trust boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Tampering | Incorrect directional matrix causing an incompatible donor to be shown a request (or hiding a compatible one) | mitigate | Values transcribed verbatim from spec §3.1; Record<BloodType,BloodType[]> enforces all 8 keys exist; acceptance asserts O- (all) and AB+ (only AB+) anchors |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (07-RESEARCH §Package Legitimacy Audit — none added); no install task present |
</threat_model>

<verification>
- COMPATIBLE_REQUEST_TYPES exported, typed Record<BloodType, BloodType[]>
- O- → all 8; AB+ → only AB+ (directional correctness anchors)
- npm run build passes for blood.ts
</verification>

<success_criteria>
- [ ] COMPATIBLE_REQUEST_TYPES named export added to src/blood.ts (GEO-01)
- [ ] Directional values match spec §3.1 exactly
- [ ] Typed against existing BloodType union; all 8 keys present
</success_criteria>

<output>
Create `.planning/phases/07-data-persistence-geo-matching/07-02-SUMMARY.md` when done.
</output>
