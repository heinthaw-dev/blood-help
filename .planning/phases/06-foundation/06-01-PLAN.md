---
phase: 06-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - BACK-01
must_haves:
  truths:
    - "PostGIS extension exists in the Supabase project under the extensions schema"
    - "All 5 enums (blood_type, request_status, response_status, urgency, lang) exist"
    - "All 5 tables (profiles, device_tokens, blood_requests, request_responses, donations) exist with all columns and constraints per blood-help-spec.md §4"
    - "The partial unique index one_open_request_per_user exists on blood_requests"
    - "All foreign key constraints and cascade rules are in place"
  artifacts:
    - path: "(Supabase project — Postgres schema)"
      provides: "5 enums, 5 tables, PostGIS extension, unique partial index"
      contains: "profiles, device_tokens, blood_requests, request_responses, donations"
  key_links:
    - from: "profiles.id"
      to: "auth.users(id)"
      via: "FK reference with on delete cascade"
      pattern: "references auth.users(id) on delete cascade"
    - from: "blood_requests.requester_id"
      to: "profiles(id)"
      via: "FK reference with on delete cascade"
      pattern: "references profiles(id) on delete cascade"
---

<objective>
Deploy the full Postgres schema to the Supabase project using MCP apply_migration: PostGIS extension, 5 enums, 5 tables with all columns/constraints/indexes.

Purpose: This is the foundational data layer. Every subsequent phase reads and writes real rows. Nothing can be built on the React side until this schema exists.
Output: All 5 tables live in the Supabase project exactly as specified in blood-help-spec.md §4.
</objective>

<execution_context>
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.planning/ROADMAP.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/.planning/phases/06-foundation/06-CONTEXT.md
@/Users/bhoneak/Desktop/Learning/VibeCodeTour/blood-help-old/blood-help-spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable PostGIS and create all enums</name>
  <read_first>
    blood-help-spec.md §4 (the canonical SQL schema — read before touching anything)
    .planning/phases/06-foundation/06-RESEARCH.md Pattern 10 (schema migration order)
    .planning/phases/06-foundation/06-CONTEXT.md D-15, D-16 (use MCP apply_migration; data model is fixed)
  </read_first>
  <action>
    Use the Supabase MCP apply_migration tool to run migration named "enable_postgis_and_enums".

    The migration SQL must:
    1. Enable PostGIS under the extensions schema: `create extension if not exists postgis with schema "extensions";`
    2. Create all 5 enums in this exact order (no deviations from spec §4):
       - `create type blood_type as enum ('A+','A-','B+','B-','O+','O-','AB+','AB-');`
       - `create type request_status as enum ('active','fulfilled','cancelled','expired');`
       - `create type response_status as enum ('responding','declined');`
       - `create type urgency as enum ('urgent','today');`
       - `create type lang as enum ('my','en');`

    Per D-15: use `apply_migration`, not the Supabase CLI. Per D-16: the exact enum values are fixed from spec §4 — do not add, remove, or rename any value.
  </action>
  <verify>
    Use Supabase MCP execute_sql: `SELECT extname FROM pg_extension WHERE extname = 'postgis';`
    Use Supabase MCP execute_sql: `SELECT typname FROM pg_type WHERE typtype = 'e' ORDER BY typname;`
    Expected: postgis in extensions; 5 rows returned: blood_type, lang, request_status, response_status, urgency.
  </verify>
  <acceptance_criteria>
    - `pg_extension` contains a row with extname = 'postgis'
    - `pg_type` WHERE typtype = 'e' returns exactly 5 rows: blood_type, lang, request_status, response_status, urgency
    - Migration completes without error from apply_migration tool
  </acceptance_criteria>
  <done>PostGIS enabled; all 5 enums exist in the Supabase project</done>
</task>

<task type="auto">
  <name>Task 2: Create all 5 tables, constraints, and the partial unique index</name>
  <read_first>
    blood-help-spec.md §4 (full table DDL — every column, type, default, and constraint)
    .planning/phases/06-foundation/06-RESEARCH.md Pitfall 8 (partial unique index MUST be WHERE status = 'active')
    .planning/phases/06-foundation/06-CONTEXT.md D-15, D-16
  </read_first>
  <action>
    Use the Supabase MCP apply_migration tool to run migration named "create_tables".

    Create tables in this order (FKs require this sequence):
    1. `profiles` — primary table, references auth.users(id) on delete cascade
    2. `device_tokens` — FK to profiles(id) on delete cascade
    3. `blood_requests` — FK to profiles(id) on delete cascade for requester_id
    4. `request_responses` — FKs to blood_requests(id) and profiles(id) on delete cascade; unique (request_id, donor_id)
    5. `donations` — FK to blood_requests(id) on delete SET NULL; FK to profiles(id) on delete cascade for donor_id; FK to profiles(id) on delete SET NULL for recipient_id

    After all tables: create the partial unique index exactly as specified:
    `create unique index one_open_request_per_user on blood_requests (requester_id) where status = 'active';`

    Copy every column, data type, default, and nullability constraint verbatim from blood-help-spec.md §4. Per D-16: no deviations.

    The `expires_at` column on `blood_requests` is `timestamptz not null` (no default — the caller sets it to now()+24h at insert time). Do not add a default.
  </action>
  <verify>
    Use Supabase MCP list_tables to confirm all 5 tables exist.
    Use Supabase MCP execute_sql: `SELECT indexname FROM pg_indexes WHERE tablename = 'blood_requests' AND indexname = 'one_open_request_per_user';`
    Use Supabase MCP execute_sql: `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'profiles' ORDER BY ordinal_position;`
  </verify>
  <acceptance_criteria>
    - MCP list_tables returns: profiles, device_tokens, blood_requests, request_responses, donations
    - `pg_indexes` contains one_open_request_per_user for blood_requests
    - `profiles` table has all columns from spec §4: id, name, phone, blood_type, donor_code (unique), is_donor, is_available, emergency_callable, donation_count, last_donation_date, available_after, township, lat, lng, location_updated_at, language, created_at, updated_at
    - Migration completes without error
  </acceptance_criteria>
  <done>All 5 tables exist with correct schema; partial unique index on blood_requests is in place</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP → Supabase Postgres | DDL applied directly to remote project; no local test environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Schema migration SQL | mitigate | Use verbatim spec §4 DDL; D-16 locks schema to spec — no ad-hoc columns |
| T-06-02 | Elevation of Privilege | one_open_request_per_user index | mitigate | Partial unique index WHERE status='active' prevents multi-request abuse at DB level |
| T-06-03 | Information Disclosure | auth.users FK | mitigate | profiles.id references auth.users(id) on delete cascade — no orphaned profile rows |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in Wave 1 — schema-only via MCP |
</threat_model>

<verification>
After both tasks complete:
- Supabase MCP list_tables returns all 5 tables
- `SELECT extname FROM pg_extension WHERE extname = 'postgis'` returns 1 row
- `SELECT typname FROM pg_type WHERE typtype = 'e' ORDER BY typname` returns 5 rows
- `SELECT indexname FROM pg_indexes WHERE tablename = 'blood_requests'` includes one_open_request_per_user
</verification>

<success_criteria>
PostGIS extension exists. All 5 enums exist. All 5 tables exist with every column, type, default, constraint, and FK from blood-help-spec.md §4. The partial unique index one_open_request_per_user exists. No deviations from spec.
</success_criteria>

<output>
Create `.planning/phases/06-foundation/06-01-SUMMARY.md` when done
</output>
